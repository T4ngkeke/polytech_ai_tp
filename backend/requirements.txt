fastapi>=0.111.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.30
asyncpg>=0.29.0
pgvector>=0.3.0         # [v7] pgvector SQLAlchemy Vector type for DocChunk embeddings
langgraph>=0.2.0        # [v7] chat agent StateGraph (router → retrieve → synthesize)
nvidia-ml-py>=12.0      # [v7] pynvml — worker GPU-utilization gate (off-peak ingestion)
pymupdf>=1.27.0         # [v7.1] PDF-only text extraction in the ingestion worker (no OCR)
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0,<4.1                # passlib is incompatible with bcrypt >=4.1
openai>=1.30.0          # AsyncOpenAI — compatible with Ollama & Mistral API
python-dotenv>=1.0.0
python-multipart>=0.0.9 # Required for UploadFile (CSV import)
httpx>=0.27.0           # async HTTP for testing
