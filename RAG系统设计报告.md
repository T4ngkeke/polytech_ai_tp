# 本地部署 RAG 智能问答系统 — 设计与决策报告

> 日期：2026-06-19
> 范围：核心模型选型、硬件可行性、路由架构、数据策略
> 一句话定位：**用一个本地 120B MoE 模型为核心，把系统建成"离线 + 在线"两个 harness，judgment 节点用大模型，机械节点用确定性手段。**

---

## 1. 需求与现状

### 1.1 我要做的事
一个**本地部署（on-premise）**的 RAG 智能问答 / chatbot，核心诉求是数据不出本地、无按量计费、可微调。

### 1.2 我已有的东西
- **核心模型候选**：一个 ~120B 的 MoE 模型 —— Qwen3.5-122B-A10B 或 NVIDIA Nemotron 3 Super 120B。
- **一套混合路由策略**（已实现的雏形）：
  - 基础问题 → 直接回答；
  - "怎么做 / 第几题 / lab 里的某练习"类 → 先用 LLM 抽取信息，再用 grep 做 agentic 检索；
  - 概念类问题（如"什么是多线程"）→ 经典 RAG。

### 1.3 我面临的核心约束
**GPU 资源有限，无法同时常驻两个模型。** —— 这是所有架构决策的硬边界。

---

## 2. 关键认知（本项目的指导思想）

### 2.1 纯 RAG 已不是现代做法
"摘要入库、按需读全文"这类摘要索引是 **2023–2024 的进阶技巧，不是 SOTA**，研究也表明它带不来实质提升。

### 2.2 真正的现代范式：Harness Engineering
2026 年的共识是 **"Agent = Model + Harness"**。前沿模型能力已经趋同，**模型不再是瓶颈，包裹模型的 harness（运行时框架）才是**——约 65% 的 agent 项目失败源于 harness 缺陷（上下文漂移、Schema 错配、状态退化），而非模型不够强。

Harness 的职责：把模型的**概率性推理**翻译成**可靠的、确定性的行动**。

### 2.3 我的路由其实已经是 harness 雏形
"按问题类型分流"对应的正是学术上的 **Adaptive-RAG / 按需触发检索**。方向是对的，要做的是把它从"静态规则"升级成"会自检、能回退"的动态 harness。

### 2.4 数据处理 pipeline 与服务是两个对称的 harness
- **离线 harness（摄取）**：处理文档、生成 chunk、注入上下文。
- **在线 harness（服务）**：查询路由、检索、判断、生成。

**核心原则贯穿两侧：机械步骤保持确定性（便宜、可测、可复现），只在真正需要判断力的"关节"嵌入模型的 agency。**

---

## 3. 模型选型

两个候选都已发布、都为 agentic 用途调优，差异主要在长上下文、许可与生态。

| 维度 | Qwen3.5-122B-A10B | NVIDIA Nemotron 3 Super 120B |
|---|---|---|
| 发布 | 2026-02 | 2026-03 |
| 参数 | 122B 总 / 10B 激活，256 专家 | 120B 总 / 12B 激活，混合 Mamba-2 + Transformer + MoE |
| 上下文 | 262K 原生（可扩 1M） | 256K 默认，支持 1M；**1M 长度可靠性极强**（RULER@1M ≈ 91.75） |
| 多模态 | 原生多模态（文/图/视频） | 主要文本 + 推理 |
| 许可 | **Apache 2.0**（最宽松） | NVIDIA Open Model License（商用可，条款较严） |
| 生态 | 同架构家族 27B→397B，**便于分层** | 自带 agentic scaffolding 钩子 |

**选型建议：**
- 痛点是**超长文档 / 多 agent 流水线** → 倾向 **Nemotron 3 Super**。
- 需要**多模态 / 宽松许可 / 同源模型家族做分层** → 倾向 **Qwen3.5**。

---

## 4. 硬件可行性（必须先懂的 MoE 陷阱）

> **误区**：以为"只激活 10–12B，就跟跑小模型一样省显存"。
> **真相**：**激活参数决定速度，总参数决定显存**——全部 120B 权重必须常驻 VRAM，专家没被用到也得在显存里。

| 精度 | 显存（仅权重） | 现实硬件门槛 |
|---|---|---|
| BF16/FP16 | ~240 GB | 3×A100-80G 或 2×H100-80G，集群级 |
| 8-bit | ~120–135 GB | 多卡（2×80G） |
| 4-bit (Q4_K_M) | ~76.5 GB | **单张 80GB 卡即可**（H100 / A100-80G） |

另需为 **KV cache** 预留余量（随上下文长度线性增长，长上下文场景显著增加）。

**结论**：可以本地跑，但**不是消费级负载**。最低配 = 一张 80GB 数据中心卡跑 4-bit 量化版；更高精度 / 更长上下文 / 更高并发需要 2–4 卡服务器。24GB 消费卡跑不动。

---

## 5. 解决"无法同时跑两个模型"的约束

这其实是**伪问题**——大模型与小模型的活发生在**不同时间**，按时间分时即可，不必空间共存。

**方案（按推荐顺序）：**

1. **离线摄取 / 在线服务分时复用同一块卡**（最干净）
   - 阶段一（离线、一次性）：加载小模型 → 批量处理整个文档库 → 写入向量库 → 卸载。
   - 阶段二（上线、常驻）：加载 122B → 对外服务。
   - 两者永不同时占显存。摄取内部若需大小两档，就**串行两遍 pass**。

2. **干脆只用一个 122B 跑全部**（最省心）
   - 分层只是优化，不是硬要求。MoE 每次只算 10–12B，批量摘要也不算太贵。
   - **先用单模型把系统跑通，有瓶颈数据再优化。**

3. **把"小模型的活"换成非 LLM 方案**（零额外显存）
   - 查询路由 / 分类 → BERT 分类器或确定性规则（见第 6 节）。
   - 同一常驻模型用 **reasoning 开关**（Nemotron 支持）或不同 system prompt 切换"快速档 / 判断档"。

---

## 6. 路由节点：用 BERT 分类器（机械节点，零显存竞争）

查询路由 = 短文本意图分类，是编码器模型的教科书级用例，**CPU 即可跑、个位数毫秒延迟、不与 122B 抢显存**。

**模型选择：**
- 首选 **ModernBERT / DeBERTa-v3**；追求极致轻量用 **DistilBERT / MiniLM**。
- **多语言场景**（中/法/英混）务必用 **XLM-RoBERTa** 或 **multilingual-e5**，勿用纯英文 BERT。

**关键陷阱：路由错误会级联**（"自信但未核查"）。必须加 **置信度阈值 + 回退**：
> 最高类别置信度低于阈值（如 0.7）→ 回退到最安全分支（通常是 RAG），或这类少数模糊 case 才临时调 122B 裁决。

---

## 7. 训练数据：用大模型生成（合成数据 / 蒸馏）

**思路**：用核心 122B 当"老师"生成训练样本，免去人工标注——大模型 bootstrap 小模型，闭环优雅。

**术语校准（影响设计）：**
- **合成数据生成**：LLM 生成"问题 + 硬标签"，训练 BERT。
- **真·知识蒸馏（KD）**：学生拟合老师的**软标签 / 概率分布**（含"暗知识"）。
- → 对**边界 / 模糊样本**用软标签做 KD，能让分类器置信度**天然校准**，直接配合第 6 节的回退阈值；普通样本用硬标签即可。

**最大风险：LLM 造的数据"太干净"**，缺少真实用户的简短、错字、口语、多语言混写 → 分类器变脆。

**对策：**
1. 生成 prompt 显式要求**多样性**（长短、正式/口语/错字、多语言、极简关键词式）。
2. 重点造**边界 / 对抗样本**。
3. 掺入手上任何**真实查询**（哪怕几十条）。
4. 上线后**主动学习**：收集低置信真实查询 → LLM 标注 → 回灌重训。

---

## 8. 推荐架构与路线图

### 8.1 目标架构（两个 harness）

```
【离线 harness：摄取】          一次性 / 批处理，分时占用 GPU
  文档 → (判断:该怎么处理?) → 切分 → Contextual Retrieval 注入上下文
       → 混合检索索引(向量+BM25) 入库
       └ 机械步骤=确定性代码 / 小模型；判断节点=122B

【在线 harness：服务】          常驻 GPU
  查询 → BERT 路由(+置信度回退)
        ├ 基础题   → 直接回答
        ├ how-to   → 抽取 + grep agentic 检索
        └ 概念题   → 混合检索 + 重排序 → 122B 生成
        + CRAG/Self-RAG 自检：结果不佳则重检索 / 降级
```

### 8.2 分阶段落地

**第一阶段（先跑通）**
- 选定模型（Qwen3.5 或 Nemotron），用 4-bit 量化版在单张 80GB 卡上跑起来。
- 单模型先承担全部任务，验证端到端效果。
- 摄取与服务分时复用 GPU。

**第二阶段（提质）**
- RAG 分支：摘要索引的想法替换为 **Contextual Retrieval + 混合检索 + 重排序**。
- 给路由加 **CRAG/Self-RAG 自检与回退**，消除"自信但未核查"。
- 上下文装配改为**渐进式披露**（预览→按需取全文），避免 context rot。

**第三阶段（提效）**
- 用 122B 合成数据训练 **BERT 路由器**，把路由从 LLM/规则迁到零显存的小分类器。
- 边界样本做软标签 KD，校准置信度。
- 建立**主动学习**回灌闭环。
- 若批量瓶颈明显，引入同家族小模型（如 Qwen3.5-35B-A3B）做分时分层。

---

## 9. 决策清单（TL;DR）

| 问题 | 结论 |
|---|---|
| 摘要索引是 SOTA 吗 | 否，是 2024 技巧；方向应转向 harness engineering |
| 我的路由过时吗 | 不，它是现代 RAG 的精髓（Adaptive-RAG 雏形） |
| 120B 能本地跑吗 | 能；4-bit 单张 80GB 卡起步，注意全部权重需常驻显存 |
| 选哪个模型 | 长上下文→Nemotron；多模态/宽松许可/分层→Qwen3.5 |
| 跑不了两个模型怎么办 | 分时复用；或单模型跑全部；或路由用非 LLM |
| 路由能用 BERT 吗 | 能，且是正解；务必加置信度回退 |
| 没有标注数据怎么办 | 用 122B 合成 + 软标签 KD + 注入多样性 + 主动学习 |
