# Edu-LLM v7.1 — 规划方案

> **背景。** v7 版本（Agent 化 RAG、数据摄入 Worker、提示词读写能力辅导）已构建并完成后端测试（共 230 个测试）。当前 RAG 路径仍处于**朴素（Naive）**阶段：固定大小分块 → 向量化 → 写入 pgvector，并配合一个**基于规则/正则的路由**（对多语言学生非常脆弱）。v7.1 重点进行 **检索质量 + Agent 化升级**；管理员监督和大部分前端工作仍**延后**（见底部）——唯独把"教师摄入可视化"这一小块**提前**，作为阶段 1 的验收界面（阶段 6）。
>
> **锁定决策（本轮）：**
> - 路由：正则 → **多语言向量-kNN**（复用 bge-m3），后续再引入 BERT。
> - 管道：**结构感知分块 + 上下文检索（Contextual Retrieval） + 混合检索（向量+BM25） + 重排。**
> - 在线：**LLM 自评估 + 有界 Agent 循环** — 本轮全力投入（已接受 60 tps 预算）。
> - GPU：保持**单常驻模型 + 非高峰低优先级批处理**（不做加载/卸载切换）。
> - **输入 = 仅 PDF。** 老师上传前自行把幻灯片（.ppt/.pptx）导出为 PDF。Worker 里**不装 LibreOffice/python-pptx，不做 OCR** —— 抽取用 pymupdf + 一个廉价的"字符产出门"；读不出的文件退回老师（`needs_review`）。
> - **彻底砍掉 corrigé（解答）。** 120B（GPT-OSS / Qwen3.5）自己能解这个难度的习题,所以**不摄入解答**,并**删掉 `Exercise.solution` 列**。**只存习题题干。** 这一刀删掉了整个解答泄漏红线 —— 我们最大的风险面。
> - **老师有任何需求**（课程约定/方法,或某道推不出的特定答案）都写进**班级级 prompt**（3-tier 注入）—— *不是*逐题字段,*不是*摄入的 corrigé。纪律:班级 prompt 放**约定,不放整套答案表**（整套答案会撑爆常驻 prompt 并重造软泄漏面）。
>
> **红线（修订）：**
> - **租户隔离**（`lab_id`/`class_id`）在 **SQL 的 WHERE 子句强制执行,绝不放进 prompt** —— prompt 注入打不穿查询过滤。
> - **不摄入 corrigé。** 解答不存储在任何地方,只存题干。（没有可泄漏的东西。）
> - **安全放数据库,教学法放 prompt。** 苏格拉底护栏是*行为*护栏 —— 学生若破解它,危害很低（模型本来就能算出那个答案,且不存在答案库）。安全边界**绝不能**退化成 prompt 护栏。
> - 仅结构化治理（控制词表,不存关于学生的自由文本评判）。

---

## 数据现实

四类文档驱动所有路由/解析决策。**类型在上传时就已知 → 它是确定性的路由信号,我们不去猜结构。**

| 类型 | 性质 | 角色 |
|---|---|---|
| **CM 幻灯片** | 脏、**最常见**、碎片化（每页上下文稀薄） | 主战场；Contextual Retrieval 是让它可检索的关键 |
| **CM 书本** | 干净、**稀有**（多数老师不写） | 黄金冒烟夹具 —— **不是**架构模板 |
| **TD** | 习题（题集） | 习题题干抽取 |
| **TP** | 习题 **+ 代码** | 习题抽取；代码块必须保持完整 |

- **输入 = 仅 PDF。** 幻灯片由老师导出为 PDF（1 页幻灯 = 1 页 PDF）。这把摄入收敛成单一 pymupdf 路径 —— 无 `.ppt` 解析,worker 里无 LibreOffice。
- **已验证：** 三份样本 PDF（TD / TP / CM_book）都是数字原生、文本可选中、干净（每页 600–2100 字符）、带可见标题结构。"很脏的 PDF"是高估了；pymupdf 直接能处理。只有内嵌的图需要跳过/处理。
- **Corrigé 是独立的老师文档** —— 但我们**不摄入它们**（见上方决策）。

---

## 目标

在一台本地 120B、约 60 tps 的硬件上,在不违反治理红线的前提下,把 RAG 从"朴素分块存储 + 关键字路由"升级为 **仅 PDF 的结构化摄入 + 多语言路由 + 高质量混合检索 + LLM 自评估有界 Agent 循环**。

## 指导原则

- 机械步骤保持确定性；把 LLM 自主性留给真正的"判断"节点（解析干净结构是机械的；Contextual Retrieval 的上下文生成是判断）。
- **摄入决定检索质量 → 管道最先做。**
- **别让那一篇干净文档（CM 书本）定义数据模型。** 为 CM 幻灯片（最常见、碎片化的情况）设计；CM 书本只当端到端骨架/冒烟夹具。
- 自评估：评分必须低成本（引导式、短输出）；循环必须有界（≤1 轮）。
- 优先廉价的确定性网关；仅对真正模糊的案例用 LLM 自评估。
- **安全放 SQL,教学法放 prompt**（见红线）。

---

## 阶段 0 — 基础准备（并行,无依赖）

| # | 任务 | 核心要点 | 完成标准 |
|---|---|---|---|
| 0.1 | 向量维度检查 | bge-m3 为 1024 维；确认列匹配 `EMBEDDING_DIM` | 无维度不匹配 |
| 0.2 | 挑选重排模型 | bge-reranker-v2-m3（多语言,CPU/小显存） | 成功加载；已更新资源预算 |
| 0.3 | tsvector 多语言配置 | 中/法/英全文检索配置 | 分词正常 |
| 0.4 | **Mock / 评测数据集** | 覆盖全部 4 类；CM 书本 = 干净冒烟夹具（**保留其目录 TOC** 以测试目录剔除）；从真实（偏脏）PDF 手标一个小评测集,作为真实分布的标尺 | 评测集存在；baseline 可衡量 |

---

## 阶段 1 — 摄入管道重构（核心工作,最优先）

> 依赖：阶段 0。它决定后续混合检索/循环能否拿到像样的候选材料。

| # | 任务 | 核心要点 | 完成标准 |
|---|---|---|---|
| 1.0 | **解析与输入门** | **仅 PDF** → pymupdf 抽取；**不做 OCR**。廉价的**字符产出门**（每页字符数低于阈值,或乱码/非词比例高）→ 标 `needs_review` 退回老师；**绝不静默摄入垃圾** | 干净 PDF 可解析；坏 PDF 被标记而非摄入 |
| 1.1 | **类型 + audience 路由** | 按上传元数据（`CM/TD/TP`、`student/teacher`）路由 —— 确定性,不猜结构；决定走 chunk 路还是 exercise 路、是否启用 Contextual Retrieval | 按元数据路由,有单测 |
| 1.2 | **结构感知分块** | 按标题/段落/习题边界切；**习题和代码块绝不从中间切断**；**识别并丢弃 TOC（目录）** —— 否则它匹配任何 query、污染召回 | 习题/代码完整；TOC 被排除 |
| 1.3 | **上下文检索（Contextual Retrieval）** | 逐块用 LLM 生成该块在文档内的上下文,向量化前拼到头部；原文仍保留。**对 CM 幻灯片是承重墙** —— 单页幻灯是上下文稀薄的碎片,否则检索不出来 | 向量化块携带上下文；幻灯片可检索 |
| 1.4 | **习题抽取 —— 只存题干** | 抽取 `number / statement / hints` 供 agentic "习题 N" 检索。**`solution` 不抽取、不存储 —— 删掉 `Exercise.solution` 列。** 题干学生本来就能看 → 无红线风险。老师的任何残余需求（约定、某道推不出的特定答案）写进**班级 prompt**,而非逐题字段 | 习题可查；`solution` 列已删除 |
| 1.5 | **构建 BM25 列（tsvector）** | 摄入时构建,否则查询时无法混合检索 | 块具备全文索引 |
| 1.6 | 元数据丰富 | type / audience / 章节 / 页码 / 保留的 `concept` | 字段被填充 |
| 1.7 | 幂等重索引验证 | 策略变更后全量重跑 | 重跑结果一致 |

- **红线：** 不摄入任何 corrigé；不存在可泄漏的 `solution`。（取代旧的"solution 绝不进 chunk"规则 —— 现在靠*根本不存它*来强制保证。）
- **成本：** 1.3 的逐块 LLM 调用完全在**非高峰 Worker** 上运行 —— 绝不抢占学生资源。

---

## 阶段 2 — 多语言路由 + 数据循环（与阶段 1 并行）

> 无阶段 1 依赖；可同步运行。

| # | 任务 | 核心要点 | 完成标准 |
|---|---|---|---|
| 2.1 | **正则 → 向量-kNN 路由器** | 复用 bge-m3；每个意图设多语言锚点样本；余弦最近邻 | 中/法/英同义词路由一致 |
| 2.2 | **置信度阈值 + 兜底** | 最高相似度 < 阈值 → 退回最安全路径（RAG） | 模糊查询不被误路由 |
| 2.3 | **记录低置信度查询** | 累积真实多语言数据,为未来 BERT 准备 | 收集表不断填充 |

---

## 阶段 3 — 查询端：混合检索 + 重排

> 依赖：阶段 1（需要双通道索引）。

| # | 任务 | 核心要点 | 完成标准 |
|---|---|---|---|
| 3.1 | **混合融合** | 向量召回 + BM25 召回 → RRF/加权融合 | 双路成功合并 |
| 3.2 | **重排** | 在融合集上运行重排,取 Top-K | 排序有可衡量的提升 |
| 3.3 | **租户过滤放 SQL** | `lab_id`/`class_id` 在 WHERE 子句强制执行,位于融合/重排之后；**绝不做成 prompt 规则** | 跨班级测试被阻断 |

---

## 阶段 4 — LLM 自评估 + 有界 Agent 循环（收尾）

> 依赖：阶段 1 + 3（只有能检索到更好材料时,重检索才值得）。

| # | 任务 | 核心要点 | 完成标准 |
|---|---|---|---|
| 4.1 | **前置廉价网关** | 余弦阈值 / 空召回,在任何 LLM 调用前阻断明显异常 | 异常跳过 LLM |
| 4.2 | **LLM 检索自评估（引导式）** | 仅模糊情况；控制词表 1~3 Token 判定；单次批处理给所有块打分 | 判定输出极短 |
| 4.3 | **有界循环** | 失败 → 重写查询并重检索,**最多 1 轮**；再失败 → 降级直接回答 | 绝不超过 1 轮 |
| 4.4 | **（可选）生成后 Grounding 评估** | 仅高风险；失败时倾向免责声明而非重生成 | 不做全文重写 |
| 4.5 | 工具调用引导式解码 | 循环依赖稳定的工具调用 | 无畸形 JSON |

- **网关：** 4.5 本地 MoE 工具调用压力测试必须在开启循环前通过。
- **为什么引导 + 有界：** 60 tps 下打分近乎免费（输出短）；最昂贵的是**重生成**,故循环封顶 1 轮、评分保持引导/短。

---

## 阶段 5 — 验证与压力测试（发布前必过）

| # | 任务 | 为什么 |
|---|---|---|
| 5.1 | **20 并发延迟测试** | 60 tps 是单请求数；自评估放大 3~5 倍 Token 量 —— 验证 KV/并发预算 |
| 5.2 | 端到端 RAG 集成测试（PG 后端） | 整条链路连通 |
| 5.3 | 红线回归 | **跨租户**是首要线（SQL 过滤）；并断言**未摄入 corrigé**（`solution` 未被填充）、租户隔离不是 prompt 规则 |

---

## 阶段 6 — 教师摄入可视化（从延后项提前）

> 依赖：阶段 1。这是阶段 1 的**验收界面**,不是 UI 美化：文档是老师写的,所以**作者就是"处理得好不好"的真值裁判** —— 而他的检视**顺便成为质量反馈/标注信号**,反哺阶段 0.4 的评测集。

| # | 任务 | 核心要点 | 完成标准 |
|---|---|---|---|
| 6.1 | `GET /api/teacher/labs/{lab_id}/documents` | 列表 + 状态（`pending/processing/indexed/failed/needs_review`）+ 汇总（页/块/习题数）+ 警告 | 列表带状态渲染 |
| 6.2 | `GET /api/teacher/documents/{id}/chunks` | 分页返回块文本 + 页码 —— **"处理得好不好"的核心界面** | 老师能读到实际块 |
| 6.3 | `GET /api/teacher/documents/{id}/exercises` | 抽取的 `number / statement`（不存在 solution） | 习题被列出 |
| 6.4 | **DocumentManager（前端）** | 列表（状态徽章 + 汇总 chips + 警告图标）→ 详情（处理报告 + **Chunk 检视器**,代码块清晰展示）；轮询列表看 `pending→indexed` | 老师读到块、看到报告 |

- 最小可行版本：一个文档列表（状态 + 汇总）+ 一个滚动展示实际 chunk 的详情页。先不做花哨图表 —— 让作者*读到被索引的东西*。

---

## 动手顺序（增量）

1. **CM 书本（干净 PDF）** → 打通端到端竖切（摄入 → 分块 → 向量 → 检索 → 回答）+ 搭起评测脚手架。目标：能跑的 baseline + 标尺,不是漂亮输出。
2. **TD / TP / CM_book PDF** → 主力解析（1.0）+ 结构感知分块（1.2）+ 类型/audience 路由（1.1）+ 只存题干的习题抽取（1.4）。
3. **Contextual Retrieval（1.3）** → 幻灯片受益最大。
4. **教师可视化（阶段 6）** → 验收 + 反馈闭环。
5. **幻灯片**：靠*"导出为 PDF"*的上传规则解决 —— **worker 无转换工作**。想测幻灯片检索,手动导一份即可,无需任何管道。

---

## 依赖关系图

```
阶段 0 ─┬─> 阶段 1 (摄入) ─┬─> 阶段 3 (混合检索+重排) ─> 阶段 4 (自评估+循环) ─> 阶段 5
        │                 └─> 阶段 6 (教师可视化)
        └─> 阶段 2 (路由) ──┘ (并行)
```

关键路径：**0 → 1 → 3 → 4 → 5**。阶段 2 同步运行；阶段 6 紧随阶段 1。

---

## 明确延后（本轮不涉及）

- **BERT/XLM-R 路由器** — 在 2.3 收集足够真实数据、且向量-kNN 被证明不足时引入。
- **完整 CRAG/Self-RAG 多轮** — 本轮封顶 ≤1 轮。
- **`concept` 自动标注 → 自适应辅导。**
- **OCR / 扫描 PDF 支持** — 当前数据用不到；坏 PDF 退回老师。
- **Worker 侧 `.ppt`/`.pptx` 自动转换（LibreOffice）** — 由"导出为 PDF"上传规则取代；除非老师反弹,否则不做。
- **逐题 `expected_answer` 字段** — 砍掉；课程级约定写进班级 prompt。
- **最终模型选型**（取决于硬件）：长上下文 → Nemotron；多模态/宽松许可/分层 → Qwen3.5。

---

# 延后主题 — 管理员监督 + 其余前端（更远期）

> 依然有效,只是优先级低于检索升级。教师*摄入可视化*这一块已提为阶段 6；以下其余部分延后到检索质量值得呈现时再做。

## 后端 — 管理员监督对齐
管理员对用户/班级/实验/分析/会话有上帝视角,但**无法查看教师编写的 v7 数据**。添加（复用相同服务函数,绕过所有权 —— "管理员绕过所有权,无跨路由调用"）：
- `GET /api/admin/rules?class_id=&lab_id=&student_id=` — 任意教师的 3 层规则。
- `GET /api/admin/documents?class_id=&lab_id=` + `GET /api/admin/labs/{lab_id}/documents` — 所有文档及状态。
- `GET /api/admin/chat-history?class_id=&lab_id=&student_id=&session_id=` — 系统范围聊天历史。
- `GET /api/admin/learner-profiles?class_id=&lab_id=&student_id=` — 学习者画像。
- `GET /api/admin/ingestion/jobs` — 队列监控（README 已声明,**尚未构建**）。
- `DELETE /api/admin/documents/{document_id}` — 强制删除任意文档（级联分块/习题）。

## 后端 — 完成教师及聊天 v7 接口（补齐与 README 的差异）
- `DELETE /api/teacher/documents/{document_id}` — 删除教师自身文档（级联 + 清理）。
- `POST /api/teacher/documents/{document_id}/reindex` — 重新入队摄入。
- `GET /api/teacher/labs/{lab_id}/learner-profiles` + `PUT .../learner-profiles/{user_id}` — 查看并覆盖画像。
- **聊天 SSE**：结尾推送 `citations` 事件（来源 + 页码）和可选的节流 `tip` 事件。

## 前端
- `lib/api.js` + SSE：增加新接口；扩展解析器以支持尾部 `citations` + 可选 `tip`。
- 学生端 `Chat.jsx`：渲染引用（文档 + 页码）；偶尔展示辅导提示横幅；含蓄的苏格拉底护栏指示器。
- 教师端 `Teacher.jsx`：辅导面板（精力趋势 + 等级 + 覆写）—— DocumentManager 已在阶段 6。
- 管理员端 `Admin.jsx`：只读上帝视角（按 班级→实验 查看规则/文档/聊天/分析/画像）；摄入监控器。
- UX：加载/空/错误状态；轮询文档接口监听状态流转。
- 清理：残留 `v5/v6` 字符串 → v7。
