# Edu-LLM v7.0 全文件逐个拆解详尽报告 (纯大白话版)

为了让你完全看透这次 V7 升级，我们按照代码目录结构，**逐一盘点修改或新增的每一个文件**，用最接地气的大白话告诉你：**这个文件是干啥用的？为什么要有它？**

---

## 📁 一、 数据底座层 (数据库相关)

### 1. `backend/app/models.py` (大幅修改)
* **它是干啥的**：定义了数据库里存什么样的数据格式。
* **具体加了啥**：加了5张新表。
  * `Document`: 记录老师传的文件档案（叫什么名、防重复的哈希值、当前状态是“处理中”还是“完成”）。
  * `DocChunk`: 把文件切碎后的“小纸条”。里面存了一段课文内容，以及一串代表这句话意思的数学标签（叫“向量 Embedding”）。
  * `Exercise`: 把文件里的“练习题”单独抽出来存，包含题号、题目、提示，以及绝对不能外泄的**标准答案**。
  * `IngestionJob`: 派给后台打工仔的任务清单表（谁要处理哪个文件）。
  * `LearnerProfile`: 学生的“平时表现档案”。记录这个学生提问是不是很努力，还是只想着抄答案（存有平滑过的努力分数和辅导等级）。
* **大白话**：给我们的图书馆新打了几个专属的书架和档案柜。

---

## 📁 二、 后台打工仔层 (`backend/worker/` 新增文件夹)
*这一层是在一个完全独立的 Docker 容器里跑的，自己玩自己的，绝对不会抢前台聊天的网速。*

### 2. `backend/worker/main.py`
* **它是干啥的**：包工头（打工仔程序的入口和主循环）。
* **具体细节**：它一直在一个死循环里跑。每转一圈，先去问 `gpu_gate` 显卡忙不忙；如果不忙，就去 `queue.py` 领任务；领到了就叫 `ingest.py` 开始处理文件；处理完更新状态，没活干或者太忙了就休眠。
* **大白话**：每天蹲在显卡门口，只要显卡闲了，就安排工人干活切文件。

### 3. `backend/worker/queue.py`
* **它是干啥的**：安全的排队抢单系统。
* **具体细节**：提供 `claim_next_job` 功能。它用了 PostgreSQL 的 `FOR UPDATE SKIP LOCKED` 语法去抢任务。如果有个任务卡了10分钟都没结束，它会判定工人“死机”了，把任务重新放回池子里给别人做。
* **大白话**：去厨房端菜。防止两个人抢一盘菜（锁死跳过机制），也防止有人端着菜晕倒了导致菜馊了（超时重试机制）。

### 4. `backend/worker/gpu_gate.py`
* **它是干啥的**：显卡保安。保护学生聊天不被卡死。
* **具体细节**：提供 `GpuGate.should_run()` 功能。监测当前显卡利用率，并且加了“滞回机制”（低于 25% 才允许后台干活，飙到 60% 就勒令停止干活）。
* **大白话**：防止保安“反复横跳”。如果不设两个阈值，显卡刚好在 40% 时，保安会一秒开门一秒关门（来回抽风）。

### 5. `backend/worker/ingest.py`
* **它是干啥的**：真正的文档切片流水线（绞肉机）。
* **具体细节**：把完整的文件文本切开，找大模型算一下每一段的“数字标签”（Embedding 向量），再把里面的作业题抽成 JSON 格式。处理前，如果发现库里有旧的碎片，会先清空旧的再插入新的（这叫**幂等操作**）。
* **大白话**：把老师厚厚的讲义放进绞肉机，切成能被电脑快速搜到的小纸条。半路停电重做时，会先把半成品废料全扔了，保证库里干干净净。

---

## 📁 三、 智能大脑流水线 (`backend/app/agent/` 新增文件夹)
*这里抛弃了以前“盲目直连大模型”的做法，用 LangGraph 搭了一个真正的智能流水线大脑。*

### 6. `backend/app/agent/router.py`
* **它是干啥的**：前台分诊护士。
* **具体细节**：纯代码正则匹配。检测话里有没有“exercise/第几题”（去搜题库 `agentic_search`），还是“什么是/怎么做”（去搜概念库 `rag`），还是其他（闲聊 `direct`）。
* **大白话**：听你一句话，马上把你分到对应科室。不需要请最贵的主任大夫（大模型）来分诊，省钱又快。

### 7. `backend/app/agent/lazy.py` 和 `effort.py`
* **它是干啥的**：防作弊教导主任。专门鉴别伸手党。
* **具体细节**：纯代码规则检查。
  * `lazy.py` 寻找“给我答案”，并且排查**有没有**出现“我试了什么”、“我觉得是”。一旦确认是纯伸手党，直接标记为 `answer_seeking`，强制开启死守答案的“苏格拉底模式”。
  * `effort.py` 是个打分器，看你发了多少字、有没有贴报错代码。打出分数。
* **大白话**：他不看你有多聪明，只看你懒不懒。哪怕笨学生，只要写了自己试过的方法，主任就给高分；上来就要答案的，直接红牌警告。

### 8. `backend/app/agent/prompt.py`
* **它是干啥的**：打包提示词的秘书。
* **具体细节**：把人设、从库里搜到的课件、以及防作弊指令拼接成一大段话给大模型。**防作弊指令永远放在文本最末尾。**
* **大白话**：把情报和校长定下的“死规矩”装订成一个文件夹交给大模型看。因为大模型记性像金鱼，规矩必须写在最后一行，它才不敢忘。

### 9. `backend/app/agent/graph.py`
* **它是干啥的**：把上面的散件串起来的履带。
* **具体细节**：用 LangGraph 构建一张图：`起点 → tutor打分 → router分发 → 去题库/概念库找资料 → synthesize打包提示词 → 终点`。
* **大白话**：工厂流水线。把教导主任、护士、图书管理员、秘书强制按顺序串在一起，一步接一步，不许乱套。

---

## 📁 四、 内部服务层 (`backend/app/services/` 新增及修改)

### 10. `document_service.py`
* **它是干啥的**：收发室大爷（负责上传）。
* **具体细节**：把老师上传的文件存在硬盘指定目录下，然后给打工仔派单（插入一条 `IngestionJob`）。

### 11. `retrieval_service.py`
* **它是干啥的**：图书管理员（负责安全拿资料）。
* **具体细节**：提供两个方法搜资料。最狠的是：代码在 SQL 查询里被写死，**绝不取出 `solution`（答案）这一列**。
* **大白话**：哪怕学生骗过了大模型让它去找答案，管理员借出的书里，答案那一页也早就被“物理撕毁”了。绝对安全。

### 12. `learner_service.py`
* **它是干啥的**：班主任档案。
* **具体细节**：利用 EMA（指数滑动平均）算法记录学生的努力分数。不是看你某一次的表现，而是看你最近一连串表现的“趋势”。

---

## 📁 五、 对外接口与部署层 (地基修改)

### 13. `backend/app/routers/chat.py` (大幅修改)
* **干了啥**：把以前“傻瓜直连大模型”的代码删了，改成把学生的聊天扔进 `agent/graph.py` 跑流水线，拿到“打包好的包裹”后，再去问大模型。

### 14. `backend/app/routers/teacher.py` (大幅修改)
* **干了啥**：专门新开了一个供老师传课件的 HTTP 接口 `/api/teacher/labs/{id}/documents`。

### 15. `backend/app/config.py` 和 `backend/seed.py`
* **干了啥**：在配置里规定了“条形码的长度”（`EMBEDDING_DIM = 1024`）。在系统启动前（seed.py），给数据库自动装上用于搜向量的 `pgvector` 扩展。

### 16. `docker-compose.yml` (环境编排大改)
* **干了啥**：
  1. 原来的 PostgreSQL 镜像换成了带有 `pgvector` 插件的高级镜像（为了能搜向量）。
  2. 新增了一个完全平行的独立容器 `worker`，专门跑刚刚讲的打工仔代码。
  3. 加了一个共享的数据卷（Volume），保证前台存的文件，后台打工仔能读到。

### 17. `requirements.txt` (采购清单)
* **干了啥**：加了 `pgvector`（存向量的依赖库）、`langgraph`（搞大脑流水线的框架）、`nvidia-ml-py`（用来让保安看显卡利用率的工具）。
